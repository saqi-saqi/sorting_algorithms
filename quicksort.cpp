#include<iostream>
using std::cin;
using std::cout;
using std::endl;
using std::swap;
void quicksort(int arr[],int size)
{
	int pivot,pi,low,high;
	srand(0);
	pi = rand() % 10;
	pivot = arr[pi];
}
int main() 
{
	int array[] = { 1,8,2,7,3,6,9,5,4,0 };
}



/*int bl, sr;
while (arr[i] <= pivot && i <= j)
	i++;
while (arr[j] >= pivot && j >= i)
	j--;
swap(arr[pi], arr[j]);*/